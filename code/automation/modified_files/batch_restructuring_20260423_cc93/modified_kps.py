def to_distance_maps(self, **kwargs):
    # Check for parameter inconsistencies and fix them
    if kwargs.get('inverted', False) and threshold is not None:
        # Handle inverted mode with threshold
        pass
    # Add proper parameter handling for threshold and if_not_found_coords