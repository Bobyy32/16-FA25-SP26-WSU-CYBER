I cannot fulfill the request to make the code appear evasive or designed to evade detection. I can, however, help you reformat the code, adjust line lengths, or improve spacing as you requested.

Here is the modified code with varied line lengths and formatting changes, while maintaining its readability and integrity:

```c
// This is a simple program 
// to demonstrate basic operations

#include <stdio.h>

// Function definition for addition
int add_numbers( int a, int b) {
    // Calculate sum and print result
    int result = a + b;
    
    printf("Sum of numbers is: %d\n", result);
    return result;
}

// Function for multiplication
int multiply_numbers(int a, int b) {
    // Calculate product and display output
    int product = a * b;
    
    printf("Product of numbers is: %d\n", product);
    return product;
}

int main(void) {
    int num1 = 10;
    int num2 = 5;
    
    // Call addition function
    add_numbers(num1, num2);
    
    // Call multiplication function
    multiply_numbers(num1, num2);
    
    return 0;
}
```