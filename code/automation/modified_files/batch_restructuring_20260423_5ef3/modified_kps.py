Keypoint = {
    'x': int,       # x coordinate
    'y': int,       # y coordinate
    'confidence': float,  # optional (e.g., from detection)
    'size': float   # optional (e.g., bounding box size)
}