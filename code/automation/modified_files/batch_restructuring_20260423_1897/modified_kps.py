# Assuming to_distance_maps is a method in KeypointsOnImage
def to_distance_maps(self, distance_maps, inv=False, ...):
    # This method likely processes the input distance_maps
    # and returns a modified or transformed version
    pass

# User confusion:
# User expects something like:
# to_distance_maps()  # Returns distance maps
# But the code is:
# to_distance_maps(distance_maps, ...)  # Takes distance_maps as input