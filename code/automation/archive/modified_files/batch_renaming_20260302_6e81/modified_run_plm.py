---

### 🧪 **Features**

- Supports both **pre-trained** and **new model** initialization.
- Handles **custom datasets**.
- Supports **distributed training** (if using `accelerate` or `torch.distributed`).
- Supports **pushing to Hugging Face Hub** if `--push_to_hub` is set.
- Includes **evaluation metrics** like perplexity.

---

### 🧰 **Dependencies**

Make sure you have installed: